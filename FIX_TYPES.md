# Fix TypeScript Type Errors

Run this command in your backend folder:

```bash
cd C:\Users\Administrateur\Desktop\revive\backend
npm install --save-dev @types/speakeasy @types/qrcode
```

Then restart your dev server:

```bash
npm run dev
```

This will install the missing TypeScript type definitions that were added to the repository.
